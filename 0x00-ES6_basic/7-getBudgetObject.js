export default function getBudgetObject(income, gdp, capita) {
  // Use shorthand property syntax to create the budget object
  const budget = {
    income,
    gdp,
    capita,
  };

  return budget;
}
